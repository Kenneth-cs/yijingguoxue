# 易经国学堂 App 数据埋点与运营分析方案

## 一、 埋点目标与核心指标 (North Star Metrics)

针对“易经国学堂” App，我们的核心运营目标是**监测用户的学习活跃度、游戏训练的参与度，以及整体应用的使用粘性**。

**核心监控指标：**
1. **日活跃用户（DAU）与登录使用量**：每天有多少独立用户打开或使用了 App。
2. **用户留存率（次日/七日/次月）**：用户首次使用 App 后，后续持续回访的比例。
3. **学习百科渗透率**：有多少活跃用户进入了“学习百科”模块并点击了相关内容。
4. **游戏训练参与度**：各类型游戏训练的点击量、参与人次及完成情况。
5. **运营位转化率**：首页 Banner 等运营推广位的点击人数与点击率。

---

## 二、 用户标识与基础指标 (User ID & DAU/Retention)

由于目前 App **还没有做账号登录体系**，我们需要通过设备特征来生成唯一用户标识，以确保行为数据能准确串联。

### 1. 独立用户 ID 生成与事件绑定逻辑
- **生成规则**：通过读取用户的设备唯一设备号（例如 iOS 的 `IDFV`），将其作为该用户的唯一身份标识（`userId`）。为了和以后的真实注册用户做区分，可以加上前缀，例如 `device_[IDFV]`。
- **事件绑定逻辑**：所有触发的埋点事件（无论是点击 Banner、浏览百科还是玩游戏），在上报时**必须全局携带此生成的 `userId`**。这样统计后台就能把所有零散的动作轨迹精准串联、绑定到这个“设备用户”身上，形成完整的用户画像。
- **后续演进**：未来上线手机号或微信登录后，在用户登录成功的那一刻，上报一个“账号绑定”事件，将真实的账号 ID 与这个 `device_xxx` 关联，历史数据即可无缝继承。

### 2. DAU 与留存率计算逻辑 (基于 User ID)
- **DAU (每日活跃用户) 计算逻辑**：直接统计当天触发了任意上报事件的总次数或设备数（根据需求，无需对 `userId` 进行严格去重，代表当天的活跃频次或基础活跃量）。
- **新增用户去重逻辑**：在统计“新增用户”时，必须对 `userId` 进行去重。如果后台在当天第一次接收到某个全新的 `userId`，则记为 1 个新增用户。
- **留存率计算逻辑**：
  - **新增日**：后台第一次接收到某个全新的 `userId` 的那一天（需去重）。
  - **次日/七日留存**：该 `userId` 在新增日之后的第 1 天（或第 7 天），是否有产生**任意**新的事件上报。如果有，则算作留存。

---

## 三、 核心埋点事件设计 (Event Tracking Plan)

事件命名规范建议采用：`模块_动作_对象`。

### 3.1 基础活跃 (App Launch)
用于统计基础的打开情况（作为 DAU 计算的兜底事件）。

| 事件 ID | 事件名称 | 触发时机 | 核心参数 (Parameters) |
| :--- | :--- | :--- | :--- |
| `app_launch` | App启动/打开 | 每次用户冷启动或从后台切换到前台时 | `is_first_open` (true/false 是否当日首次打开) |

### 3.2 学习百科模块 (Wiki & Learning)
用于分析多少人点击学习百科，以及最受欢迎的百科内容。

| 事件 ID | 事件名称 | 触发时机 | 核心参数 (Parameters) |
| :--- | :--- | :--- | :--- |
| `wiki_view_page` | 浏览百科主页 | 点击进入“学习百科”模块的主列表页时 | 无 |
| `wiki_click_article` | 点击百科内容 | 在列表中点击某一条具体的百科/课程时 | `article_id` (文章ID)<br>`article_name` (文章名称)<br>`category` (所属分类，如基础/进阶) |

### 3.3 游戏与训练模块 (Games & Training)
用于分析用户练习各种游戏训练的参与度和完成率。

| 事件 ID | 事件名称 | 触发时机 | 核心参数 (Parameters) |
| :--- | :--- | :--- | :--- |
| `game_click_entrance` | 点击游戏入口 | 点击某个具体的游戏训练入口时 | `game_type` (游戏类型/名称，如六爻排盘/八卦记忆等) |
| `game_start_training` | 开始游戏训练 | 成功进入游戏界面并开始练习时 | `game_type` (游戏类型/名称) |
| `game_finish_training` | 完成游戏训练 | 单局游戏或练习结束时 | `game_type` (游戏类型)<br>`score` (得分/准确率)<br>`duration_sec` (练习耗时，秒) |

### 3.4 首页、练习中心与运营位 (Home, Practice & Banner)
用于分析 Banner 等运营位的导流效果。

| 事件 ID | 事件名称 | 触发时机 | 核心参数 (Parameters) |
| :--- | :--- | :--- | :--- |
| `home_click_banner` | 点击首页Banner | 点击首页轮播图或特定Banner时 | `banner_id` (Banner ID)<br>`banner_name` (Banner名称/标题)<br>`target_url` (跳转目标) |
| `practice_click_banner` | 点击练习中心Banner | 点击练习中心（游戏训练页）的Banner时 | `banner_id` (Banner ID)<br>`banner_name` (Banner名称/标题)<br>`target_url` (跳转目标) |

---

## 四、 iOS 端数据上报 API 接入说明

基于现有的数据上报接口服务，请在 iOS (Swift) 项目中使用以下代码进行事件上报。**我们在代码中加入了基于 IDFV 生成的 User ID 绑定逻辑**。

### 1. 核心配置参数
- **API Base URL**: `https://www.superindividual.youqukeji.cn`
- **API Key**: `cplt_960de2824798753f015d4d1e17666b97cbbc61c1491cec5339db567b9713b4a6`
- **Project ID**: `cmoydglev00j5oyc4penjkdkm`

### 2. Swift 接入示例代码

将以下代码封装为一个单例的 `EventTracker` 类，它会在初始化时生成并缓存设备级的 `userId`，并在每次上报时全局携带：

```swift
import Foundation
import UIKit

class EventTracker {
    static let shared = EventTracker()
    
    private let kApiBase = "https://www.superindividual.youqukeji.cn"
    private let kApiKey  = "cplt_960de2824798753f015d4d1e17666b97cbbc61c1491cec5339db567b9713b4a6"
    private let kProjectId = "cmoydglev00j5oyc4penjkdkm"
    
    // 当前用户的唯一标识 (在没有登录系统前，使用 DeviceID 生成)
    private var currentUserId: String = ""
    private var deviceId: String = ""
    
    private init() {
        // 获取设备 IDFV
        self.deviceId = UIDevice.current.identifierForVendor?.uuidString ?? UUID().uuidString
        // 生成基于设备的虚拟 User ID
        self.currentUserId = "device_\(self.deviceId)"
    }
    
    /// 当未来有了真正的登录系统后，可以调用此方法更新 userId
    func updateUserId(with realUserId: String) {
        self.currentUserId = realUserId
    }
    
    /// 统一事件上报方法
    func trackEvent(eventId: String, eventName: String, params: [String: Any]? = nil) {
        let body: [String: Any] = [
            "projectId": kProjectId,
            "userId": self.currentUserId,     // 核心修改：绑定生成的唯一用户ID
            "deviceId": self.deviceId,
            "eventId": eventId, 
            "eventName": eventName,
            "params": params ?? [:],
            "appVersion": Bundle.main.infoDictionary?["CFBundleShortVersionString"] as? String ?? "1.0",
            "osVersion": UIDevice.current.systemVersion,
            "occurredAt": ISO8601DateFormatter().string(from: Date())
        ]
        
        guard let url = URL(string: "\(kApiBase)/api/events") else { return }
        var req = URLRequest(url: url)
        req.httpMethod = "POST"
        req.setValue("application/json", forHTTPHeaderField: "Content-Type")
        req.setValue("Bearer \(kApiKey)", forHTTPHeaderField: "Authorization")
        
        do {
            req.httpBody = try JSONSerialization.data(withJSONObject: body)
            URLSession.shared.dataTask(with: req).resume()
        } catch {
            print("Event tracking serialization error: \(error)")
        }
    }
}
```

### 3. 具体埋点调用示例

```swift
// 1. App 启动时记录 (DAU 兜底)
EventTracker.shared.trackEvent(
    eventId: "app_launch",
    eventName: "App启动",
    params: ["is_first_open": true]
)

// 2. 记录点击首页 Banner
EventTracker.shared.trackEvent(
    eventId: "home_click_banner",
    eventName: "点击首页Banner",
    params: ["banner_name": "双十一易经课程特惠", "banner_id": "b_1001"]
)

// 3. 记录完成游戏训练 (后台会自动将此行为归属到 currentUserId 名下)
EventTracker.shared.trackEvent(
    eventId: "game_finish_training",
    eventName: "完成游戏训练",
    params: ["game_type": "八卦记忆", "score": 95, "duration_sec": 120]
)
```

---

## 五、 运营分析场景举例 (如何使用这些数据？)

1. **基础活跃度与新增统计**：
   - 关注每天总的活跃上报量（DAU，不去重），了解应用的整体热度。同时通过去重 `userId` 精准统计每天的**新增用户数**。
2. **留存与核心行为的关联分析**：
   - 提取触发过 `game_start_training` 的 `userId` 集合，对比未触发过的 `userId` 集合。如果玩过游戏训练的用户群，其 7日留存率 明显更高，说明“游戏化训练”是提升应用粘性的核心功能，应该在首页给更大的曝光权重。
3. **转化漏斗排查**：
   - 建立基于同一 `userId` 的行为漏斗：`game_click_entrance` (点击入口) -> `game_start_training` (开始) -> `game_finish_training` (完成)。
   - 后台通过追踪相同的 `userId` 是否连续触发了这三个事件，计算出每一步的流失率，从而判断是加载太慢劝退了用户，还是游戏难度太高导致无法完成。
4. **用户生命周期洞察**：
   - 圈选出某个具体的 `userId`，可以通过他的事件流看到他的行为轨迹，比如：启动 App -> 点了 Banner -> 看了百科文章 -> 玩了一把六爻排盘 -> 退出。有助于具象化理解用户行为模式。